{
  "name": "tuyul-ndase-ireng",
  "version": "0.3.0",
  "private": true,
  "description": "BTC/USDT scanner with deterministic virtual paper execution",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "replay": "tsx src/replay.ts",
    "replay:book": "tsx src/replay-book.ts",
    "replay:paper": "tsx src/replay-paper.ts",
    "paper": "tsx src/paper.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "node --import tsx --test src/exchanges/*.test.ts src/scanner/*.test.ts src/recording/*.test.ts src/metrics/*.test.ts src/replay/*.test.ts src/timing/*.test.ts src/app/*.test.ts src/paper/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "@types/ws": "^8.18.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
